import React from 'react';
import { ContextConsumer } from '../context';
const MyAccount = (props) => {
    return (
        <div>

            {/* <ContextConsumer>
                {(v) => {
                    return (<div>{v.isLogedIn}</div>)
                }}
            </ContextConsumer> */}
            myacounts

        </div>
    )
}
export default MyAccount;